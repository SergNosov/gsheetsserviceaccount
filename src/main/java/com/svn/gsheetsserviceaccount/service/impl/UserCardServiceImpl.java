package com.svn.gsheetsserviceaccount.service.impl;

import com.svn.gsheetsserviceaccount.model.UserCard;
import com.svn.gsheetsserviceaccount.service.UserCardService;

import java.util.Optional;

public class UserCardServiceImpl implements UserCardService {

    @Override
    public Optional<UserCard> save(UserCard contact) {
        return Optional.empty();
    }
}
